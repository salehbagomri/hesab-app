# نظام الترجمة الاحترافي - Hesab App

## 📋 نظرة عامة

تطبيق حساب يدعم نظام ترجمة ديناميكي ومرن بالكامل بين العربية والإنجليزية.

## 🎯 المبادئ الأساسية

### 1. **تخزين المفاتيح وليس النصوص**
```dart
// ✅ صحيح
operationType: 'addition'  // مفتاح الترجمة

// ❌ خطأ
operationType: 'الجمع'     // نص مترجم
operationType: 'Addition'  // نص مترجم
```

### 2. **الترجمة الديناميكية عند العرض**
```dart
// ✅ استخدام localizations.translate()
Text(localizations.translate(item.operationType))

// ❌ عرض النص المحفوظ مباشرة
Text(item.operationType)
```

### 3. **عدم استخدام نصوص مباشرة**
```dart
// ✅ صحيح
Text(localizations.usefulTip)

// ❌ خطأ
Text('معلومة مفيدة')
Text('Useful Tip')
```

## 🏗️ البنية المعمارية

### ملفات الترجمة
```
assets/localization/
├── ar.json    # الترجمة العربية (157+ مفتاح)
└── en.json    # الترجمة الإنجليزية (157+ مفتاح)
```

### نظام AppLocalizations
```dart
// الوصول للترجمة
final localizations = AppLocalizations.of(context);

// استخدام getter محدد
localizations.addition  // "الجمع" أو "Addition"

// استخدام مفتاح ديناميكي
localizations.translate('addition')  // "الجمع" أو "Addition"
```

## 📝 قواعد الحفظ في قاعدة البيانات

### HistoryItem Model
```dart
@HiveField(2)
late String operationType; // ✅ مفتاح الترجمة فقط

// أمثلة للقيم المحفوظة:
'addition'           // ✅
'quadraticEquations' // ✅
'trigonometry'       // ✅

// قيم خاطئة:
'الجمع'              // ❌
'Addition'           // ❌
```

### Level Values
```dart
@HiveField(1)
late String level;

// القيم الصحيحة:
'elementary'  // ✅
'middle'      // ✅
'high'        // ✅
```

## 🔄 عملية الحفظ والعرض

### 1. عند الحفظ (result_screen.dart)
```dart
await ref.read(historyProvider.notifier).addToHistory(
  level: widget.level,                      // 'elementary'
  operationType: widget.operationType.translationKey,  // 'addition'
  input: inputText,
  result: explanation.result,
  explanation: explanationJson,
);
```

### 2. عند العرض (history_screen.dart)
```dart
Text(
  localizations.translate(item.operationType),  // يترجم حسب اللغة الحالية
  style: Theme.of(context).textTheme.titleSmall?.copyWith(
    color: AppColors.primary,
    fontWeight: FontWeight.w600,
  ),
)
```

## 🌍 دعم تعدد اللغات

### التبديل بين اللغات
```dart
// في settings_provider.dart
void changeLanguage(String languageCode) {
  state = state.copyWith(language: languageCode);
  _saveSettings();
}

// الاستخدام
ref.read(settingsProvider.notifier).changeLanguage('ar');  // العربية
ref.read(settingsProvider.notifier).changeLanguage('en');  // الإنجليزية
```

### إعادة الترجمة التلقائية
- عند تغيير اللغة، جميع النصوص تُعاد ترجمتها تلقائياً
- السجل يُعرض باللغة الحالية بغض النظر عن لغة الحفظ
- لا حاجة لإعادة تحميل أو refresh

## 📊 مثال عملي كامل

### السيناريو: حفظ عملية جمع

#### 1. المستخدم يختار عملية الجمع
```dart
// اللغة: العربية
operationType = OperationType.addition
operationType.translationKey = 'addition'
```

#### 2. الحفظ في قاعدة البيانات
```dart
HistoryItem(
  operationType: 'addition',  // ✅ المفتاح فقط
  level: 'elementary',
  input: '5 , 3',
  result: '8',
  ...
)
```

#### 3. العرض باللغة العربية
```dart
localizations.translate('addition')  // ➜ "الجمع"
localizations.translate('elementary') // ➜ "إعدادي"
```

#### 4. المستخدم يغير اللغة للإنجليزية
```dart
// نفس البيانات المحفوظة، ترجمة مختلفة
localizations.translate('addition')  // ➜ "Addition"
localizations.translate('elementary') // ➜ "Elementary"
```

## ✅ قائمة التحقق للمطورين

### عند إضافة عملية جديدة:
- [ ] إضافة enum في `OperationType`
- [ ] إضافة مفتاح في `ar.json`
- [ ] إضافة مفتاح في `en.json`
- [ ] إضافة getter في `AppLocalizations`
- [ ] استخدام `translationKey` عند الحفظ
- [ ] استخدام `translate()` عند العرض

### عند إضافة نص جديد:
- [ ] إضافة المفتاح في `ar.json`
- [ ] إضافة المفتاح في `en.json`
- [ ] إضافة getter في `AppLocalizations`
- [ ] استخدام `localizations.keyName` في الكود

### عند عرض بيانات محفوظة:
- [ ] استخدام `translate()` للعمليات
- [ ] استخدام `translate()` للمستويات
- [ ] عدم عرض النصوص المحفوظة مباشرة

## 🛡️ الحماية من الأخطاء

### 1. مفتاح غير موجود
```dart
String translate(String key) {
  return _localizedStrings[key] ?? key;  // يعيد المفتاح نفسه
}
```

### 2. لغة غير مدعومة
```dart
static const List<Locale> supportedLocales = [
  Locale('ar'),
  Locale('en'),
];
```

### 3. بيانات قديمة
- النظام يدعم البيانات القديمة تلقائياً
- إذا كان المفتاح موجود، سيترجم
- إذا كان نص قديم، سيعرض كما هو

## 📈 مميزات النظام

✅ **مرونة كاملة** - إضافة لغات جديدة بسهولة  
✅ **أداء عالي** - الترجمة من الذاكرة  
✅ **صيانة سهلة** - كل الترجمات في مكان واحد  
✅ **تحديث فوري** - تغيير اللغة بدون إعادة تشغيل  
✅ **متوافق مع RTL/LTR** - دعم كامل للاتجاهات  
✅ **آمن من الأخطاء** - معالجة المفاتيح المفقودة  

## 🔍 استكشاف الأخطاء

### المشكلة: النص لا يترجم
```dart
// ✅ تحقق من:
1. المفتاح موجود في ar.json و en.json
2. getter موجود في AppLocalizations
3. استخدام localizations.translate() أو localizations.keyName
4. عدم استخدام const مع Text
```

### المشكلة: السجل يظهر بلغة خاطئة
```dart
// ✅ تحقق من:
1. حفظ المفتاح وليس النص
2. استخدام translate() عند العرض
3. operationType يحتوي على 'addition' وليس 'الجمع'
```

## 📚 الملفات المعدلة

- ✅ `result_screen.dart` - حفظ المفتاح بدلاً من النص
- ✅ `history_screen.dart` - ترجمة العمليات ديناميكياً
- ✅ `history_detail_screen.dart` - ترجمة العنوان
- ✅ `home_screen.dart` - ترجمة العمليات الأخيرة
- ✅ `history_item.dart` - توثيح نوع البيانات

## 🎓 أفضل الممارسات

1. **دائماً احفظ المفاتيح** في قاعدة البيانات
2. **دائماً ترجم عند العرض** باستخدام AppLocalizations
3. **لا تستخدم نصوص مباشرة** في الكود
4. **اختبر بكلا اللغتين** قبل الإطلاق
5. **وثق التغييرات** في ملفات JSON

---

**آخر تحديث:** 11 ديسمبر 2025  
**الإصدار:** 1.0.0  
**الحالة:** ✅ جاهز للإنتاج
